{
  "name": "preset-lite",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "description": "TauriTavern third-party extension: incremental rendering and idle token recounts for the chat-completion preset/prompt panel.",
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "happy-dom": "^20.14.5",
    "typescript": "^5.9.3"
  }
}
